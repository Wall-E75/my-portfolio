import styles from '@/styles/FullStackProjects.module.css'

function FullStackProjects() {

    return(
        <main>
            <h1>FUll Stack</h1>
        </main>

    );
};

export default FullStackProjects;